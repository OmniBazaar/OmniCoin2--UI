import React, { Component } from 'react';

import './escrow.scss';

export default class Escrow extends Component {
  render() {
    return (
      <div>
                Escrow
      </div>
    );
  }
}
