import React, {Component} from 'react';

import './support.scss';

export default class Support extends Component {
    render() {
        return (
            <div>
                Support
            </div>
        );
    }
}