import React, {Component} from 'react';

import './mail.scss';

export default class Mail extends Component {
    render() {
        return (
            <div>
                Mail
            </div>
        );
    }
}